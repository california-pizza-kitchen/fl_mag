<%= form_for(@blogger) do |f| %> 
  
  <% if @blogger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blogger.errors.count, "error") %> prohibited this blogger from being saved:</h2>

      <ul>
      <% @blogger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label 'Add a student:' %><br />
  <%= f.text_field :name %><br />

  <%= f.label 'Blog XML Address:' %> <br />
  <%= f.text_field :feed_xml %><br />

  <%= f.label 'Semester:' %> <br />
  <%= f.select :semester, (0..10) %><br /><br />

  <%= f.submit 'Create Blogger' %>
<% end %>