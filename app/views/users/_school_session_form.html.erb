<hr>
<h4><strong>Add a session:</strong></h4>
<%= form_for(@school_session,:html => {class:"form-group"}) do |f| %> 
  
  <% if @school_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school_session.errors.count, "error") %> prohibited this Session from being saved:</h2>

      <ul>
      <% @school_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    
    <div class="col-xs-3">
      <%= f.label 'Session Name:' %><br />
      <%= f.text_field :name, :class => 'form-control' %><br />
    </div>

    <%= f.submit 'Create Session', :class => 'btn btn-primary' %>

  </div>

<% end %>